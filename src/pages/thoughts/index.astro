---
import { getCollection } from 'astro:content';
import BaseHead from '../../components/BaseHead.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../../consts';
import FormattedDate from '../../components/FormattedDate.astro';

const thoughts = (await getCollection('thoughts')).sort(
	(a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf()
);
---

<!doctype html>
<html lang="zh-CN">
	<head>
		<BaseHead title={`思路记录 | ${SITE_TITLE}`} description={SITE_DESCRIPTION} />
		<style>
			main {
				width: 960px;
				max-width: calc(100% - 2em);
				margin: auto;
				padding: 3em 1em;
			}
			h1 {
				text-align: center;
				margin-bottom: 0.5em;
				background: linear-gradient(135deg, #fff 0%, #00d4ff 100%);
				-webkit-background-clip: text;
				-webkit-text-fill-color: transparent;
				background-clip: text;
			}
			.subtitle {
				text-align: center;
				color: rgba(255, 255, 255, 0.6);
				margin-bottom: 3rem;
			}
			ul {
				display: flex;
				flex-wrap: wrap;
				gap: 2rem;
				list-style-type: none;
				margin: 0;
				padding: 0;
			}
			ul li {
				width: calc(50% - 1rem);
			}
			ul li * {
				text-decoration: none;
				transition: 0.2s ease;
			}
			.card {
				background: linear-gradient(135deg, rgba(22, 33, 62, 0.8) 0%, rgba(26, 26, 46, 0.6) 100%);
				border-radius: 16px;
				padding: 1.5rem;
				border: 1px solid rgba(255, 255, 255, 0.1);
				transition: all 0.3s ease;
				height: 100%;
			}
			.card:hover {
				transform: translateY(-4px);
				border-color: var(--accent);
				box-shadow: 0 8px 32px rgba(0, 212, 255, 0.15);
			}
			.card a {
				display: block;
				color: inherit;
			}
			.title {
				margin: 0 0 0.8rem;
				color: #fff;
				font-size: 1.2rem;
				line-height: 1.4;
			}
			.date {
				margin: 0;
				color: rgba(255, 255, 255, 0.5);
				font-size: 0.9rem;
			}
			.category {
				display: inline-block;
				background: linear-gradient(135deg, var(--accent) 0%, var(--accent-dark) 100%);
				color: #000;
				padding: 0.2rem 0.8rem;
				border-radius: 12px;
				font-size: 0.85rem;
				font-weight: 600;
				margin-top: 0.8rem;
			}
			.status {
				display: inline-block;
				margin-left: 0.5rem;
				font-size: 0.8rem;
				padding: 0.1rem 0.5rem;
				border-radius: 4px;
			}
			.status.draft { background: #fbbf24; color: #000; }
			.status.published { background: #10b981; color: #fff; }
			.status.archived { background: #6b7280; color: #fff; }
			.empty {
				text-align: center;
				color: rgba(255, 255, 255, 0.4);
				padding: 3rem;
				background: rgba(255, 255, 255, 0.03);
				border-radius: 16px;
				border: 1px dashed rgba(255, 255, 255, 0.1);
			}
			@media (max-width: 720px) {
				main {
					padding: 1em;
				}
				ul li {
					width: 100%;
				}
				.card {
					padding: 1.2rem;
				}
			}
		</style>
	</head>
	<body>
		<Header />
		<main>
			<h1>💡 思路记录</h1>
			<p class="subtitle">想法、笔记、灵感收集</p>
			<section>
				<ul>
					{
						thoughts.map((post) => (
							<li>
								<div class="card">
									<a href={`/thoughts/${post.id}/`}>
										<h4 class="title">{post.data.title}</h4>
										<p class="date"><FormattedDate date={post.data.pubDate} /></p>
										{post.data.category && <span class="category">{post.data.category}</span>}
										<span class={`status ${post.data.status}`}>{post.data.status}</span>
									</a>
								</div>
							</li>
						))
					}
					{thoughts.length === 0 && <li class="empty">暂无思路记录</li>}
				</ul>
			</section>
		</main>
		<Footer />
	</body>
</html>
