---
import { getCollection } from 'astro:content';
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../consts';
import FormattedDate from '../components/FormattedDate.astro';

// 获取各分类最新内容
const latestNews = (await getCollection('news')).sort((a, b) => 
	b.data.pubDate.valueOf() - a.data.pubDate.valueOf()
).slice(0, 3);

const latestStocks = (await getCollection('stocks')).sort((a, b) => 
	b.data.pubDate.valueOf() - a.data.pubDate.valueOf()
).slice(0, 1);

const latestLife = (await getCollection('life')).sort((a, b) => 
	b.data.pubDate.valueOf() - a.data.pubDate.valueOf()
).slice(0, 3);

const latestThoughts = (await getCollection('thoughts')).sort((a, b) => 
	b.data.pubDate.valueOf() - a.data.pubDate.valueOf()
).slice(0, 3);
---

<!doctype html>
<html lang="zh-CN">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
		<style>
			.hero {
				text-align: center;
				padding: 5rem 2rem;
				background: linear-gradient(135deg, rgba(0, 212, 255, 0.1) 0%, rgba(0, 153, 204, 0.05) 50%, transparent 100%);
				border-bottom: 1px solid rgba(0, 212, 255, 0.2);
				position: relative;
				overflow: hidden;
			}
			.hero::before {
				content: '';
				position: absolute;
				top: 0;
				left: 0;
				right: 0;
				bottom: 0;
				background: radial-gradient(ellipse at center, rgba(0, 212, 255, 0.1) 0%, transparent 70%);
				pointer-events: none;
			}
			.hero h1 {
				font-size: 3.5rem;
				margin: 0 0 1rem;
				background: linear-gradient(135deg, #fff 0%, #00d4ff 50%, #0099cc 100%);
				-webkit-background-clip: text;
				-webkit-text-fill-color: transparent;
				background-clip: text;
				text-shadow: 0 0 40px rgba(0, 212, 255, 0.3);
				position: relative;
			}
			.hero p {
				font-size: 1.3rem;
				color: rgba(255, 255, 255, 0.7);
				margin: 0;
				position: relative;
			}
			.section {
				margin: 4rem 0;
			}
			.section-header {
				display: flex;
				justify-content: space-between;
				align-items: center;
				margin-bottom: 1.5rem;
				border-bottom: 2px solid var(--accent);
				padding-bottom: 0.8rem;
			}
			.section-header h2 {
				margin: 0;
				font-size: 1.6rem;
				color: #fff;
			}
			.section-header a {
				color: var(--accent);
				text-decoration: none;
				font-size: 0.95rem;
				padding: 0.5em 1em;
				border: 1px solid var(--accent);
				border-radius: 20px;
				transition: all 0.2s ease;
			}
			.section-header a:hover {
				background: var(--accent);
				color: #000;
			}
			.card-list {
				display: grid;
				grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
				gap: 1.5rem;
			}
			.card {
				background: linear-gradient(135deg, rgba(22, 33, 62, 0.8) 0%, rgba(26, 26, 46, 0.6) 100%);
				border-radius: 16px;
				padding: 1.5rem;
				border: 1px solid rgba(255, 255, 255, 0.1);
				transition: all 0.3s ease;
			}
			.card:hover {
				transform: translateY(-4px);
				border-color: var(--accent);
				box-shadow: 0 8px 32px rgba(0, 212, 255, 0.15);
			}
			.card a {
				text-decoration: none;
				color: inherit;
				display: block;
			}
			.card h3 {
				margin: 0 0 0.8rem;
				font-size: 1.15rem;
				color: #fff;
				line-height: 1.4;
			}
			.card p {
				margin: 0;
				color: rgba(255, 255, 255, 0.6);
				font-size: 0.95rem;
				line-height: 1.6;
			}
			.card .meta {
				display: flex;
				gap: 1rem;
				margin-top: 1.2rem;
				font-size: 0.85rem;
				color: rgba(255, 255, 255, 0.5);
				flex-wrap: wrap;
			}
			.empty {
				text-align: center;
				color: rgba(255, 255, 255, 0.4);
				padding: 3rem;
				background: rgba(255, 255, 255, 0.03);
				border-radius: 16px;
				border: 1px dashed rgba(255, 255, 255, 0.1);
			}
			/* 今日说股样式 */
			.stocks-header {
				border-bottom-color: #00ff88;
			}
			.stocks-header h2 {
				background: linear-gradient(135deg, #00ff88 0%, #00d4ff 100%);
				-webkit-background-clip: text;
				-webkit-text-fill-color: transparent;
				background-clip: text;
			}
			.stock-card {
				border-left: 4px solid #00ff88;
			}
			.stock-card:hover {
				border-color: #00ff88;
				box-shadow: 0 8px 32px rgba(0, 255, 136, 0.15);
			}
			.stock-index {
				background: linear-gradient(135deg, #00ff88 0%, #00d4ff 100%);
				color: #000;
				padding: 0.5rem 1rem;
				border-radius: 8px;
				font-weight: 600;
				font-size: 0.95rem;
				margin: 1rem 0;
				display: inline-block;
			}
			.tag {
				background: linear-gradient(135deg, rgba(0, 255, 136, 0.2) 0%, rgba(0, 212, 255, 0.2) 100%);
				color: #00ff88;
				padding: 0.2rem 0.6rem;
				border-radius: 12px;
				font-size: 0.85rem;
			}
			@media (max-width: 720px) {
				.hero {
					padding: 3rem 1rem;
				}
				.hero h1 {
					font-size: 2.2rem;
				}
				.hero p {
					font-size: 1.1rem;
				}
				.card-list {
					grid-template-columns: 1fr;
				}
			}
		</style>
	</head>
	<body>
		<Header />
		
		<div class="hero">
			<h1>🚀 我的数字花园</h1>
			<p>记录新闻、生活与思考</p>
		</div>
		
		<main>
			<!-- 最新新闻 -->
			<section class="section">
				<div class="section-header">
					<h2>📰 最新新闻</h2>
					<a href="/news">查看全部 →</a>
				</div>
				<div class="card-list">
					{latestNews.length > 0 ? latestNews.map(post => (
						<div class="card">
							<a href={`/news/${post.id}/`}>
								<h3>{post.data.title}</h3>
								<p>{post.data.description}</p>
								<div class="meta">
									<FormattedDate date={post.data.pubDate} />
									{post.data.source && <span>来源: {post.data.source}</span>}
								</div>
							</a>
						</div>
					)) : <div class="empty">暂无新闻</div>}
				</div>
			</section>

			<!-- 今日说股 -->
			<section class="section">
				<div class="section-header stocks-header">
					<h2>📈 今日说股</h2>
					<a href="/stocks">查看全部 →</a>
				</div>
				<div class="card-list">
					{latestStocks.length > 0 ? latestStocks.map(post => (
						<div class="card stock-card">
							<a href={`/stocks/${post.id}/`}>
								<h3>{post.data.title}</h3>
								<p>{post.data.description}</p>
								{post.data.marketIndex && (
									<div class="stock-index">{post.data.marketIndex}</div>
								)}
								<div class="meta">
									<FormattedDate date={post.data.pubDate} />
									{post.data.topGainers && (
										<span class="tag">📈 {post.data.topGainers.slice(0, 3).join('、')}</span>
									)}
								</div>
							</a>
						</div>
					)) : <div class="empty">暂无股票分析</div>}
				</div>
			</section>

			<!-- 最新生活 -->
			<section class="section">
				<div class="section-header">
					<h2>🌟 生活随记</h2>
					<a href="/life">查看全部 →</a>
				</div>
				<div class="card-list">
					{latestLife.length > 0 ? latestLife.map(post => (
						<div class="card">
							<a href={`/life/${post.id}/`}>
								<h3>{post.data.title}</h3>
								<p>{post.data.description}</p>
								<div class="meta">
									<FormattedDate date={post.data.pubDate} />
									{post.data.mood && <span>心情: {post.data.mood}</span>}
								</div>
							</a>
						</div>
					)) : <div class="empty">暂无生活记录</div>}
				</div>
			</section>

			<!-- 最新思路 -->
			<section class="section">
				<div class="section-header">
					<h2>💡 思路记录</h2>
					<a href="/thoughts">查看全部 →</a>
				</div>
				<div class="card-list">
					{latestThoughts.length > 0 ? latestThoughts.map(post => (
						<div class="card">
							<a href={`/thoughts/${post.id}/`}>
								<h3>{post.data.title}</h3>
								<p>{post.data.description}</p>
								<div class="meta">
									<FormattedDate date={post.data.pubDate} />
									{post.data.category && <span>{post.data.category}</span>}
								</div>
							</a>
						</div>
					)) : <div class="empty">暂无思路记录</div>}
				</div>
			</section>
		</main>
		
		<Footer />
	</body>
</html>
