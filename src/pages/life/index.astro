---
import { getCollection } from 'astro:content';
import BaseHead from '../../components/BaseHead.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../../consts';
import FormattedDate from '../../components/FormattedDate.astro';

const lifePosts = (await getCollection('life')).sort(
	(a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf()
);
---

<!doctype html>
<html lang="zh-CN">
	<head>
		<BaseHead title={`生活随记 | ${SITE_TITLE}`} description={SITE_DESCRIPTION} />
		<style>
			main {
				width: 960px;
				max-width: calc(100% - 2em);
				margin: auto;
				padding: 3em 1em;
			}
			h1 {
				text-align: center;
				margin-bottom: 0.5em;
				background: linear-gradient(135deg, #fff 0%, #00d4ff 100%);
				-webkit-background-clip: text;
				-webkit-text-fill-color: transparent;
				background-clip: text;
			}
			.subtitle {
				text-align: center;
				color: rgba(255, 255, 255, 0.6);
				margin-bottom: 3rem;
			}
			ul {
				display: flex;
				flex-wrap: wrap;
				gap: 2rem;
				list-style-type: none;
				margin: 0;
				padding: 0;
			}
			ul li {
				width: calc(50% - 1rem);
			}
			ul li * {
				text-decoration: none;
				transition: 0.2s ease;
			}
			.card {
				background: linear-gradient(135deg, rgba(22, 33, 62, 0.8) 0%, rgba(26, 26, 46, 0.6) 100%);
				border-radius: 16px;
				padding: 1.5rem;
				border: 1px solid rgba(255, 255, 255, 0.1);
				transition: all 0.3s ease;
				height: 100%;
			}
			.card:hover {
				transform: translateY(-4px);
				border-color: var(--accent);
				box-shadow: 0 8px 32px rgba(0, 212, 255, 0.15);
			}
			.card a {
				display: block;
				color: inherit;
			}
			.title {
				margin: 0 0 0.8rem;
				color: #fff;
				font-size: 1.2rem;
				line-height: 1.4;
			}
			.date {
				margin: 0;
				color: rgba(255, 255, 255, 0.5);
				font-size: 0.9rem;
			}
			.mood {
				display: inline-block;
				margin-top: 0.8rem;
				font-size: 1.2rem;
			}
			.location {
				display: inline-block;
				margin-top: 0.5rem;
				color: var(--accent);
				font-size: 0.9rem;
			}
			.empty {
				text-align: center;
				color: rgba(255, 255, 255, 0.4);
				padding: 3rem;
				background: rgba(255, 255, 255, 0.03);
				border-radius: 16px;
				border: 1px dashed rgba(255, 255, 255, 0.1);
			}
			@media (max-width: 720px) {
				main {
					padding: 1em;
				}
				ul li {
					width: 100%;
				}
				.card {
					padding: 1.2rem;
				}
			}
		</style>
	</head>
	<body>
		<Header />
		<main>
			<h1>🌟 生活随记</h1>
			<p class="subtitle">记录身边发生的事情</p>
			<section>
				<ul>
					{
						lifePosts.map((post) => (
							<li>
								<div class="card">
									<a href={`/life/${post.id}/`}>
										<h4 class="title">{post.data.title}</h4>
										<p class="date"><FormattedDate date={post.data.pubDate} /></p>
										{post.data.location && <span class="location">📍 {post.data.location}</span>}
										{post.data.mood && <span class="mood">{post.data.mood}</span>}
									</a>
								</div>
							</li>
						))
					}
					{lifePosts.length === 0 && <li class="empty">暂无生活记录</li>}
				</ul>
			</section>
		</main>
		<Footer />
	</body>
</html>
